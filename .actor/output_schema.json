{
    "actorOutputSchemaVersion": 1,
    "title": "Output Schema",
    "properties": {
        "resultsDataset": {
            "title": "Results dataset",
            "type": "string",
            "template": "{{links.apiDefaultDatasetUrl}}"
        }
    }
}
